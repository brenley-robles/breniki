import 'package:flutter/material.dart';

class Project4Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Project 4'),
      ),
      body: Center(
        child: Text('Details for Project 4'),
      ),
    );
  }
}
