import 'package:flutter/material.dart';

class Project2Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Project 2'),
      ),
      body: Center(
        child: Text('Details for Project 2'),
      ),
    );
  }
}
