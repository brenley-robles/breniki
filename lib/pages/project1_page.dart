import 'package:flutter/material.dart';

class Project1Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Project 1'),
      ),
      body: Center(
        child: Text('Details for Project 1'),
      ),
    );
  }
}
